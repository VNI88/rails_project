<h1>Usuários</h1>
<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Email</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @consumers.each do |consumer| %>
      <tr>
        <td><%= consumer.name %></td>
        <td><%= consumer.email %></td>
        <td><%= link_to 'Ver detalhes', consumer %></td>
      </tr>
    <% end %>
  </tbody>
</table>
